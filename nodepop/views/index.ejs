<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <h3>Datos devueltos api: </h3>
      <% datos.forEach(function(dato) { %>
        <p><%= dato.nombre + ' ' + dato.venta + ' ' + dato.precio + ' ' + dato.foto + ' ' + dato.tags%></p>
      <% }) %>
  </body>
</html>
